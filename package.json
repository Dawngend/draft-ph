{
  "name": "seal",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "domains/*",
    "design-system"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @seal/portal",
    "dev:portal": "npm run dev --workspace @seal/portal",
    "build": "npm run build --workspace @seal/portal",
    "start": "npm run start --workspace @seal/portal",
    "typecheck": "npm run typecheck --workspace @seal/portal"
  }
}
